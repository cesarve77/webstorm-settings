import React, {Component} from 'react';
import PropTypes from 'prop-types'

class ${NAME} extends Component {
    constructor(props) {
        super(props)
    }

    render() {
        return (
            <div>
            </div>
        )
    }

    static defaultProps = {}

    static propTypes = {}
}

export default ${NAME}
