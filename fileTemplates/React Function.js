import React from 'react';
import PropTypes from 'prop-types'

const ${NAME} = ({})=>{
        return (
            <div>
            </div>
        )
    }
}

${NAME}.displayName = '${NAME}'


export default ${NAME}
